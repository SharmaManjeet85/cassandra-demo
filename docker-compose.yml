services:

  cassandra:
    image: cassandra:5.0
    container_name: cassandra-db
    ports:
      - "9042:9042"
    environment:
      CASSANDRA_CLUSTER_NAME: PhotoCluster
    volumes:
      - cassandra_data:/var/lib/cassandra

volumes:
  cassandra_data: